# CS2 Price Database Requirements
# Core dependencies for the CS2 price collection system

# HTTP client for Steam API requests with proxy support
aiohttp>=3.8.0

# Environment variable management
python-dotenv>=0.19.0

# Optional: SOCKS proxy support (uncomment if needed)
# aiohttp-socks>=0.7.1

# Standard library modules (included with Python):
# - asyncio (async/await support)
# - json (JSON parsing)
# - logging (logging functionality) 
# - datetime (date/time handling)
# - typing (type hints)
# - os (operating system interface)
# - re (regular expressions)
# - time (time functions)
# - dataclasses (data class support)
# - random (random number generation)